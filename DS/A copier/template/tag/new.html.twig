{% extends 'base.html.twig' %}

{% block title %}Nouveau Tag{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="mb-4 text-light">Créer un nouveau Tag</h1>

        <div class="bg-dark p-4 rounded shadow-sm">
            {{ include('tag/_form.html.twig', {'button_label': 'Créer'}) }}
        </div>

        <a href="{{ path('app_tag_index') }}" class="btn btn-outline-light mt-3">
            <i class="bi bi-arrow-left"></i> Retour à la liste
        </a>
    </div>
{% endblock %}
