<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Mon Application Symfony{% endblock %}</title>

    <!-- Bootswatch Zephyr (clair) -->
    <link href="https://cdn.jsdelivr.net/npm/bootswatch@5.3.2/dist/zephyr/bootstrap.min.css" rel="stylesheet">

    <style>
        /* Navbar sombre fixe en haut */
        .navbar {
            background-color: #212529 !important;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1030;
        }
        .navbar .navbar-brand,
        .navbar .nav-link {
            color: #f8f9fa !important;
        }
        .navbar .nav-link:hover {
            color: #adb5bd !important;
        }

        /* Pour que le contenu ne soit pas caché sous la navbar fixe */
        body {
            padding-top: 56px; /* Hauteur approximative de la navbar */
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        /* Conteneur principal qui prend tout l'espace entre navbar et footer */
        .container {
            flex: 1 0 auto;
            padding-top: 20px;
            padding-bottom: 20px;
        }

        /* Footer collé en bas */
        footer {
            background-color: #f8f9fa;
            color: #212529;
            text-align: center;
            padding: 15px 0;
            border-top: 1px solid #dee2e6;
            font-size: 14px;
            flex-shrink: 0;
        }

        /* Alert spacing */
        .alert {
            margin-top: 20px;
        }
    </style>

    {% block stylesheets %}{% endblock %}
</head>

<body>
<nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_dashboard') }}">📝 R209</a>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item"><a class="nav-link" href="{{ path('app_dashboard') }}">Dashboard</a></li>
                <li class="nav-item"><a class="nav-link" href="/note">Notes</a></li>
                <li class="nav-item"><a class="nav-link" href="/tag">Tags</a></li>
                <li class="nav-item"><a class="nav-link" href="/etat">États</a></li>
                <li class="nav-item"><a class="nav-link" href="/vie/note">VieNote</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('app_login') }}">Login</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('app_register') }}">Register</a></li>
            </ul>
        </div>
    </div>
</nav>

<div class="container">
    {% for message in app.flashes('success') %}
        <div class="alert alert-success">{{ message }}</div>
    {% endfor %}

    {% block body %}{% endblock %}
</div>

<footer>
    © {{ "now"|date("Y") }} R209 - Sanchez Durante Hugo 🚀
</footer>

{% block javascripts %}{% endblock %}
</body>
</html>
